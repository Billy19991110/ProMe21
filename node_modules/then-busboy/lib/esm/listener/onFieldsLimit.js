import createError from "../util/requestEntityTooLarge.js";
const createOnFieldsLimit = ({ limits }, entries) => () => {
    entries.emit("error", createError(`Fields limit exceeded: Available up to ${limits.fields} fields.`));
};
export default createOnFieldsLimit;
