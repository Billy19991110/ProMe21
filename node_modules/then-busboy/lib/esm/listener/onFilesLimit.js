import createError from "../util/requestEntityTooLarge.js";
const createOnFilesLimit = ({ limits }, entries) => () => {
    entries.emit("error", createError(`Files limit exceeded: Available up to ${limits.files} files.`));
};
export default createOnFilesLimit;
